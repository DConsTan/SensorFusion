<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="boris.mambap.int_acc_posit">
    <uses-feature android:name="android.hardware.sensor.TYPE_LINEAR_ACCELERATION" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.TYPE_GRAVITY" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.TYPE_MAGNETIC_FIELD" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.TYPE_ROTATION_VECTOR" android:required="true" />
    <uses-permission
        android:name="android.permission.INTERNET"
        android:required="true" />
    <uses-permission
        android:name="android.hardware.wifi"
        android:required="true" />
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        android:required="true" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>